メンバーリスト

<% @members.each do |member| %>
  <div>
  <%= "#{member.nickname} : #{member.email}" %>
  </div>
<% end %>

<ul>
  <li><%= link_to 'このメンバーとグループを作成', new_group_path %></li>
  <li><%= link_to '他のユーザーも検索する', search_users_path %></li>
</ul>